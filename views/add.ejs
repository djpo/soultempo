<h3>Search for some songs!</h3>

<form action="/results" method="POST" role="form">

<% for (var i = 0; i < 3; i++) { %>

  <div class="form-group">
    <label for="">Artist</label>
    <input type="text" class="form-control" id="artist<%= i %>" placeholder="artist" name="artist_<%= i %>">
  </div>
  <div class="form-group">
    <label for="">Title</label>
    <input type="text" class="form-control" id="title<%= i %>" placeholder="title" name="title_<%= i %>">
  </div>
  <ln>

<% } %>

  <button type="submit" class="btn btn-primary">Confirm</button>
</form>
