<form action="/summary" method="GET" role="form">
  <legend>Add a few songs!</legend>

<% for (var i = 0; i < 2; i++) { %>

  <div class="form-group">
    <label for="">Artist</label>
    <input type="text" class="form-control" id="artist<%= i %>" placeholder="artist" name="artist_<%= i %>">
  </div>

  <div class="form-group">
    <label for="">Title</label>
    <input type="text" class="form-control" id="title1" placeholder="title" name="title_<%= i %>">
  </div>

  <ln>

<% } %>

  <button type="submit" class="btn btn-primary">Submit</button>
</form>
