<h2>Add songs!</h2>

<form action="/results" method="GET" role="form">

<% for (var i = 0; i < 2; i++) { %>

  <div class="form-group">
    <label for="">Artist</label>
    <input type="text" class="form-control" id="artist<%= i %>" placeholder="artist" name="artist_<%= i %>" value="david bowie">
  </div>
  <div class="form-group">
    <label for="">Title</label>
    <input type="text" class="form-control" id="title<%= i %>" placeholder="title" name="title_<%= i %>" value="changes">
  </div>
  <ln>

<% } %>

  <button type="submit" class="btn btn-primary">Submit</button>
</form>