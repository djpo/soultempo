project 2 workflow
____________________
in progress

send song_ids from results page to database
	with confirmation button

change /results GET routes to POST?
____________________
general workflow

make db work
	validations?
	associations?
		models.user.hasMany(models.favorite);
		models.favorite.belongsTo(models.user);

authorization

summary page
	make second API request for each song id in results
		return artist, title, tempo
		render summary page w/ this info

async.waterfall vs. async.concat?

style

____________________
database (name: soultempo)

create users table
(id)	username	password	soul_tempo
1		coolguy5	?			85
2		808-is-me	?			125
3		Ferdinand	?			105

create favorites table
(id)	u_id	song_id		tempo (float)
1		1		6789		52.885
2		1		589059		89.6741
3		1		123			105.002			
4		4		6789		142.4
5		2		123			80
__________
sequelize model:create --name user --attributes username:string,password:string,soul_tempo:integer

sequelize model:create --name favorite --attributes u_id:integer,song_id:string,tempo:float

then
sequelize db:migrate
____________________
extra

allow song add with incomplete fields
	if no title entered
		search by artist only
	if no artist entered
		search by title only

user can edit favorites
	delete from list > delete row from favorites table

user can edit song additions before submission
	if found, fields turn into returned songs from EN
	if not found, fields turn red
